<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.customer_farmer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CustomerLoginActivity"></activity>
        <activity android:name=".FarmerLoginActivity" />
        <activity android:name=".CustomerActivity" />
        <activity android:name=".FarmerActivity" />
        <activity android:name=".edit_customer" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ViewOrderCustomer" />
        <activity android:name=".OrderProducts" />
        <activity android:name=".vieworders_farmer" />
        <activity android:name=".ViewListContents" />
        <activity android:name=".edit_farmer" />
        <activity android:name=".productActivity" />
        <activity android:name=".farmer_login" />
        <activity android:name=".farmer_signup" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
